@model WebApp.ModelUI.PagedResult<WebApp.Models.Account>
@using System.Text.Json
@using System.Text.Json.Serialization


@{
    ViewData["Title"] = "Quản lý tài khoản";
}

<style>
    .badge-role {
        padding: 4px 12px;
        border-radius: 20px;
        font-size: 14px;
        font-weight: 600;
        display: inline-block;
    }

    .badge-admin {
        background-color: #ffe5e5;
        color: #d60000;
    }

    .badge-editor {
        background-color: #e6f0ff;
        color: #0051ff;
    }

    .badge-user {
        background-color: #e6ffea;
        color: #0f8a20;
    }
</style>
<div class="container-fluid">
    <div class="card">
        <div class="card-body">
            <div class="row mb-1">
                <!-- Search -->
                <div class="col-md-3">
                    <div class="input-group rounded">
                        <input type="search" class="form-control" id="searchInput" placeholder="Tìm kiếm..." aria-label="Search" aria-describedby="search-addon" />
                        <span class="input-group-text border-0 btn btn-primary" id="search-addon">
                            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-search" viewBox="0 0 16 16">
                                <path d="M11.742 10.344a6.5 6.5 0 1 0-1.397 1.398h-.001q.044.06.098.115l3.85 3.85a1 1 0 0 0 1.415-1.414l-3.85-3.85a1 1 0 0 0-.115-.1zM12 6.5a5.5 5.5 0 1 1-11 0 5.5 5.5 0 0 1 11 0"/>
                            </svg>
                        </span>
                    </div>
                </div>
                <div class="col-md-6">
                </div>
                <!-- Add button -->
                @* <div class="col-md-3 text-end">
                    <button type="button" class="btn btn-success" data-bs-toggle="modal" data-bs-target="#modalCreatephongBan">
                        Thêm 
                    </button>
                </div> *@
            </div>

            <div class="table-responsive">
                <table class="table table-bordered rounded">
                    <thead>
                        <tr class="text-center bg-light">
                            <th>STT</th>
                            <th>Tên đăng nhập</th>
                            <th>Email</th>
                            <th>Trạng thái</th>
                            <th class="sticky-column">Hành động</th>
                        </tr>
                    </thead>
                    <tbody id="phongBanTable">
                        @{
                            int stt = 1;
                        }
                        @foreach (WebApp.Models.Account item in Model.Items)
                        {
                            <tr>
                                <td class="text-center">@stt</td>
                                <td class="tenPhongBan">@item.Username</td>
                                <td class="truongPhong">@item.Email</td>
                                <td class="soDienThoai text-center">@if (item.Role == "Admin")
                                {
                                    <span class="badge-role badge-admin">Admin</span>
                                }
                                else if (item.Role == "Employee")
                                {
                                    <span class="badge-role badge-editor">Employee</span>
                                }
                                else
                                {
                                    <span class="badge-role badge-user">User</span>
                                }</td>
                                <td class="text-center d-flex sticky-column"> <!-- Sticky column -->
                                    <button class="btn btn-sm btn-primary btn-edit btnEdit" data-item="@JsonSerializer.Serialize(item)" data-id="@item.Id">
                                        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-pencil-square" viewBox="0 0 16 16">
                                            <path d="M15.502 1.94a.5.5 0 0 1 0 .706L14.459 3.69l-2-2L13.502.646a.5.5 0 0 1 .707 0l1.293 1.293zm-1.75 2.456-2-2L4.939 9.21a.5.5 0 0 0-.121.196l-.805 2.414a.25.25 0 0 0 .316.316l2.414-.805a.5.5 0 0 0 .196-.12l6.813-6.814z"/>
                                            <path fill-rule="evenodd" d="M1 13.5A1.5 1.5 0 0 0 2.5 15h11a1.5 1.5 0 0 0 1.5-1.5v-6a.5.5 0 0 0-1 0v6a.5.5 0 0 1-.5.5h-11a.5.5 0 0 1-.5-.5v-11a.5.5 0 0 1 .5-.5H9a.5.5 0 0 0 0-1H2.5A1.5 1.5 0 0 0 1 2.5z"/>
                                        </svg>
                                    </button>
                                  
                                    <button class="btn btn-sm btn-danger btn-delete" data-id="@item.Id">
                                        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-trash3" viewBox="0 0 16 16">
                                            <path d="M6.5 1h3a.5.5 0 0 1 .5.5v1H6v-1a.5.5 0 0 1 .5-.5M11 2.5v-1A1.5 1.5 0 0 0 9.5 0h-3A1.5 1.5 0 0 0 5 1.5v1H1.5a.5.5 0 0 0 0 1h.538l.853 10.66A2 2 0 0 0 4.885 16h6.23a2 2 0 0 0 1.994-1.84l.853-10.66h.538a.5.5 0 0 0 0-1zm1.958 1-.846 10.58a1 1 0 0 1-.997.92h-6.23a1 1 0 0 1-.997-.92L3.042 3.5zm-7.487 1a.5.5 0 0 1 .528.47l.5 8.5a.5.5 0 0 1-.998.06L5 5.03a.5.5 0 0 1 .47-.53Zm5.058 0a.5.5 0 0 1 .47.53l-.5 8.5a.5.5 0 1 1-.998-.06l.5-8.5a.5.5 0 0 1 .528-.47M8 4.5a.5.5 0 0 1 .5.5v8.5a.5.5 0 0 1-1 0V5a.5.5 0 0 1 .5-.5"/>
                                        </svg>
                                    </button>
                                </td>
                            </tr>
                            stt++;
                        }
                    </tbody>
                    <tfoot>
                        <td colspan="10" class="text-left">
                                <nav aria-label="Page navigation">
                                    <ul class="pagination justify-content-right">
                                        <!-- Nút "Trước" -->
                                        <li class="page-item @(Model.Page == 1 ? "disabled" : "")">
                                            <a class="page-link" href="?page=@(Model.Page - 1)">>></a>
                                        </li>

                                        <!-- Các số trang -->
                                        @for (int i = 1; i <= Model.TotalPages; i++)
                                        {
                                            <li class="page-item @(i == Model.Page ? "active" : "")">
                                                <a class="page-link" href="?page=@i">@i</a>
                                            </li>
                                        }

                                        <!-- Nút "Sau" -->
                                        <li class="page-item @(Model.Page == Model.TotalPages ? "disabled" : "")">
                                            <a class="page-link" href="?page=@(Model.Page + 1)"><<</a>
                                        </li>
                                    </ul>
                                </nav>
                            </td>
                    </tfoot>
                </table>
            </div>

        </div>
    </div>
</div>
<link rel="stylesheet" href="../assets/css/styles.min.css" />
<script src="~/js/login.js"></script>